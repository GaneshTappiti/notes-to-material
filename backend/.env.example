# Copy to .env and fill in real secrets (never commit your actual .env)
GOOGLE_API_KEY=
JWT_SECRET=change_me
DAILY_CALL_LIMIT=0
# Optional DB (fallback to SQLite if unset)
# DATABASE_URL=postgresql+psycopg://user:pass@host:5432/dbname
